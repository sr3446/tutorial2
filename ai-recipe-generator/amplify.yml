version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ai-recipe-generator/amplify
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ai-recipe-generator/amplify/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**

