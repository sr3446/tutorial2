version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ai-recipe-generator
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist  # Or 'build' depending on your framework
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
